# Exe research notes

Read-only static inspection of the hash-identified `Emperor[EN].exe` recorded in `DESIGN.md`. These notes are for agents and developers; they are not runtime dependencies.

| Note | Topic |
| --- | --- |
| [construction-bbuttons.md](./construction-bbuttons.md) | Construction menu `China_Interface` Bbuttons; `0x1192B88` UI-record writers (`0x546EA0`/`0x547580`); `0x4A5960` invert (early bases not via UI records); building model blob; exhausted negatives; catalog evidence classes |

Policy reminders:

- Never copy the proprietary executable into the repository, app bundle, or tests.
- Classify conclusions as `confirmed` / `inferred` / `unknown`.
- Update `DESIGN.md` when a recovered rule changes a player-visible or cross-system contract.
